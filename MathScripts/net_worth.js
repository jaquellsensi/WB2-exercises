// Creating a calculator to determine Networth
// the formula would be assets - debts = Networth

let assets = 100;
let debts = 80;

const Networth = assets - debts; //formula for networth

console.log("Your networth is " + Networth.toFixed(2)); // calculation of net